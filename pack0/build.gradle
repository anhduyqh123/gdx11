// In the asset pack’s build.gradle file:
plugins {
  id 'com.android.asset-pack'
}

assetPack {
    packName = "pack0" // Directory name for the asset pack
    dynamicDelivery {
        deliveryType = "on-demand"
    }
}